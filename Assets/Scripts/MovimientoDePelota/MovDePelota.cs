using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(Rigidbody))]
public class MovDePelota : MonoBehaviour {

	Vector3 Direction; 	//Vector unitario de direccion
	float Fuerza;		//Magnitud del vector

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
